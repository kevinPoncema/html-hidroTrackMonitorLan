import { TooltipPosition } from "./tooltipPosition";
/**
 * Render tooltip box in the page
 *
 * @api private
 */
export declare const placeTooltip: (tooltipLayer: HTMLElement, arrowLayer: HTMLElement, targetElement: HTMLElement, position: TooltipPosition, positionPrecedence: TooltipPosition[], showStepNumbers?: boolean, autoPosition?: boolean, tooltipClassName?: string, hintMode?: boolean) => void;
